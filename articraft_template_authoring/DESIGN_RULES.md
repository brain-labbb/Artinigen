# Legacy Design Rules Pointer

本文件只保留为兼容入口，避免旧链接失效。

新类别模板 authoring 时不要把本目录当成独立规则来源。进入
`TEMPLATE_AFTER_REVIEW` 后必须读取并遵循仓库根目录：

```text
TEMPLATE_DESIGN_RULES.md
TEMPLATE_AUTHORING_AGENT.md
```

唯一初步完成规则：

```text
sweep-pipeline verdict=pass
```
